<li>
    <p><%= event.date %></p>
    <h6>Updated at: <%= event.updated_at %></h6>
    <h6>Creator: <%= event.user.name %></h6>
    <% if @logged_in %>
        <h6><%= User.find_by(id: event.user_id).name %></h6>
    <% end %>
<li>
